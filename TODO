- Check 1 = enabled, 0 = disable in form data - make them bool and convert to int on send
- move _base and constanst
- for devices models think about make baseclass for all same fields validation
- move count from profile to base file
- move BaseFormData from devices to root level
- add logger in inside of print
